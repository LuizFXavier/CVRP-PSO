#pragma once
#include <vector>
#include <string>
#include <unordered_map>
using namespace std;
class Scanner
{
private:
    
public:
    static unordered_map<string, string> read_config(string config_file);
    
};
